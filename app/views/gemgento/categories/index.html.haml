.categories-nav
	- @categories.each do |category|
		.category
			= link_to category.name, category_path(category)
				
	.products-container
		- @products.each do |p|
			.products
				%br
				=  link_to p.attribute_value('name'), product_path(p)
